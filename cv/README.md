# CV source

`main.tex` here is the source of truth for the CV.

Edit it, commit, push — the `Build CV` workflow compiles it and publishes
`cv.pdf` to the repo root, which is what the portfolio links to.

Keep `photo.jpeg` in this folder; `main.tex` references it.
